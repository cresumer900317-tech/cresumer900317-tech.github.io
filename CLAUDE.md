# 친구들.com — 길드 웹 프론트

메이플 키우기 "친구패밀리" 길드(친구들·친구둘·친구삼·친구넷·친구닷, 길드원 약 100명) 웹사이트.

## 구조

| 면 | 위치 | 배포 |
|---|---|---|
| **웹(여기)** | `~/dev/guild-web` · repo `cresumer900317-tech.github.io` | GitHub Pages (`git push`) |
| 백엔드 | `~/dev/guild-backend` · repo `guild-backend` | Railway (FastAPI + Supabase) |
| 앱 | `guild-app-54` (Expo SDK54) | iOS App Store + Google Play |

정적 HTML + `assets/js/*.js`. 셋 다 같은 Railway API를 공용으로 쓴다.
백엔드에 기능을 추가하면 웹·앱 양쪽에서 쓸 수 있다.

전략: **웹 = 서버 유틸 + 쇼윈도 + 모집 퍼널 / 앱 = 길드원 유틸**

## 주의사항

- 캐릭터명 ↔ 랭킹 데이터 매칭 시 **한글 NFC 정규화 필수** (`.normalize('NFC')`)
- 크롤링 원본은 mgf.gg 랭킹 목록 페이지. 상세 페이지(`character.php`)는 JS 렌더링이라 못 읽음
- 넥슨 오픈 API에 **메이플 키우기는 없다** (본편/M/SEA/TW만) — 캐릭터 스탯 자동 조회 불가
- Railway 배포는 push 후 빌드 큐 10~20분 (Hobby)

---

## 진행 중: 데미지 계산기 / 옵션 효율 랭킹

**목표: "현재 내 스탯에서 뭘 올리면 더 세지나"** — 스탯·잠재 옵션 효율 랭킹.
아이템 A vs B 비교는 그 응용. 절대 DPS는 부가 기능.

넥슨이 2026-08-06 공개한 공식 전투 공식·능력치 사전을 근거로 한다.

### 📖 작업 전 반드시 읽을 것

```
docs/maple-idle/
├── 01-공식-전투공식.md      넥슨 공식 가이드 전문 (원문 보존)
├── 02-능력치사전.md         능력치 28개 전문 (원문 보존)
├── 03-스테이지-몬스터.json   356 스테이지 방어/회피/HP
├── 04-설계결정.md      ★ 왜 그렇게 정했는지 — 이거 없이 작업하면 논의를 반복하게 됨
├── 05-직업-스킬데이터.md     14직업 주/부스탯, 팔라딘 수집분, 13직업 템플릿
└── 06-미해결.md        ★ 검증 대기 항목 + 기준 캐릭터 스탯(회귀 테스트용)
```

### 절대 지킬 원칙

1. **LLM에게 계산을 시키지 않는다.** AI는 스크린샷 파싱만. 계산은 결정론적 JS
2. **휴리스틱으로 계산을 덮어쓰지 않는다.** "기본공격 데미지가 정석" 같은 통설 금지
3. **스킬 계수 DB는 1레벨 기준으로만 만든다.** (2026-08-08 변경) 전 스킬을 1레벨 기준으로 수집해 직업 간 비교·밸런스 랭킹에 사용. 사용자 본인의 딜 계산은 여전히 본인 계수를 게임 보고 입력 — 1레벨 DB 값으로 대체하지 않는다
4. **전역 스탯 마스터리를 따로 받지 않는다.** 스탯창에 이미 포함 = 이중 계산
5. **전투력 Δ와 실제 딜 Δ를 나란히 표시한다.** 전투력에는 함정 항목이 많다

### 기존 코드

- `item-compare.html` · `assets/js/item-compare.js` — 아이템 비교 AI (2026-08-06 작성)
- 백엔드 `guild_backend/item_compare.py` — `POST /api/item-compare/analyze`

⚠️ 현재 구현은 **AI가 판정(`verdict`)까지 생성**해서 정확도가 낮다.
파싱 전용으로 축소하고 계산은 JS로 옮기는 게 계획. 상세는 `04-설계결정.md` 1장.
